general:
  build_dir: plugins/diindex/app
## Customize machine settings for Ember-cli
machine:
  node:
    version: 0.12.0

## Customize dependencies
dependencies:
  post: 
    - bower install
    - npm rebuild node-sass