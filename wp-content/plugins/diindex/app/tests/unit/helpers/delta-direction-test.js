import { deltaDirection } from 'diindex-ember-dev/helpers/delta-direction';
import { module, test } from 'qunit';

module('Unit | Helper | delta direction');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = deltaDirection([42]);
  assert.ok(result);
});
